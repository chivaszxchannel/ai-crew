# คู่มือติดตั้งและใช้งาน ai-crew (ภาษาไทย)

> **ตั้งค่าเร็วสุด:** ติดตั้งเสร็จแล้วพิมพ์ `/crew-config ui` จะได้หน้าตั้งค่าในเบราว์เซอร์ เลือกโมเดลจาก dropdown กดปุ่มเดียว login ให้ ดูรายละเอียดที่ [หัวข้อ 4](#4-ตั้งค่าด้วยหน้าเว็บ-แนะนำ)

ai-crew คือปลั๊กอินสำหรับ **Claude Code** ที่ทำให้โมเดลหลายตัวทำงานเป็นทีม: หัวหน้าวางแผนแตกงาน ลูกน้องโมเดลถูกกว่าเขียนโค้ดและทดสอบ แล้วส่งให้ **ผู้ตรวจต่างค่าย** (Codex CLI หรือ Gemini CLI) ตรวจบั๊ก วนแก้จนผ่าน แล้วรายงานสั้นๆ เป็นภาษาไทยว่าแต่ละโมเดลทำอะไร ไฟล์ไหนต้องอัปโหลด อะไรทดสอบแล้วจริง อะไรต้องไปกดดูเอง

---

## สารบัญ

1. [ต้องมีอะไรก่อน](#1-ต้องมีอะไรก่อน)
2. [ติดตั้งปลั๊กอิน](#2-ติดตั้งปลั๊กอิน)
3. [เตรียมเครื่องด้วย /crew-setup](#3-เตรียมเครื่องด้วย-crew-setup)
4. [ตั้งค่าด้วยหน้าเว็บ (แนะนำ)](#4-ตั้งค่าด้วยหน้าเว็บ-แนะนำ)
5. [ตั้งค่าแบบถามตอบในแชท](#5-ตั้งค่าแบบถามตอบในแชท)
6. [เริ่มใช้งานจริง](#6-เริ่มใช้งานจริง)
7. [ไฟล์ที่ปลั๊กอินสร้างในโปรเจกต์](#7-ไฟล์ที่ปลั๊กอินสร้างในโปรเจกต์)
8. [ตั้งค่าละเอียด (config)](#8-ตั้งค่าละเอียด-config)
9. [แก้ปัญหาที่พบบ่อย](#9-แก้ปัญหาที่พบบ่อย)
10. [อัปเดตและถอนการติดตั้ง](#10-อัปเดตและถอนการติดตั้ง)
11. [ข้อจำกัดที่ควรรู้ก่อนใช้](#11-ข้อจำกัดที่ควรรู้ก่อนใช้)

---

## 1. ต้องมีอะไรก่อน

| สิ่งที่ต้องมี | ใช้ทำอะไร | จำเป็นไหม |
|---|---|---|
| **Claude Code** (CLI หรือ extension ใน VS Code / JetBrains) หรือ **Claude Cowork** | ตัวรันปลั๊กอิน | จำเป็น |
| **Node.js 18 ขึ้นไป** ([nodejs.org](https://nodejs.org)) | ติดตั้ง CLI ของผู้ตรวจ | จำเป็น |
| **บัญชี ChatGPT** (Plus/Pro หรือ API key) | ใช้ Codex CLI เป็นผู้ตรวจ | ไม่จำเป็น — ถ้าไม่มี ปลั๊กอินจะใช้ Opus ตรวจแทนเอง |
| **บัญชี Google** | ใช้ Gemini CLI เป็นผู้ตรวจสำรอง | ไม่จำเป็น |
| **Git** | เฉพาะถ้าจะติดตั้งจาก GitHub | ไม่จำเป็นถ้าใช้ไฟล์ zip |

ตรวจว่ามี Node แล้วหรือยัง เปิด **Terminal** (ใน VS Code กด `` Ctrl + ` ``) แล้วพิมพ์:

```
node -v
npm -v
```

ถ้าขึ้นเลขเวอร์ชัน (เช่น `v22.18.0`) ถือว่าผ่าน ถ้าขึ้นว่าไม่รู้จักคำสั่ง ให้ติดตั้ง Node.js LTS จาก nodejs.org แล้ว **ปิด-เปิด terminal ใหม่**

---

## 2. ติดตั้งปลั๊กอิน

> **สำคัญมาก:** คำสั่งในหัวข้อนี้พิมพ์ใน **Terminal** (PowerShell / cmd / bash) ไม่ใช่ในช่องแชทของ Claude Code
> แผงแชทของ extension ใน VS Code ไม่รองรับคำสั่ง `/plugin` จะขึ้นว่า *"/plugin isn't available in this environment"*
> ส่วนคำสั่งที่ขึ้นต้นด้วย `/crew...` ให้พิมพ์ใน **ช่องแชท** ไม่ใช่ terminal (ถ้าพิมพ์ใน terminal จะขึ้น *"is not recognized as the name of a cmdlet"*)

### วิธี A — ติดตั้งจาก GitHub (แนะนำ)

repo เป็น **สาธารณะ (public)** และเป็น MIT license — ติดตั้งได้เลยโดยไม่ต้องมีบัญชี GitHub ไม่ต้องขอสิทธิ์ใคร

```
claude plugin marketplace add chivaszxchannel/ai-crew
claude plugin install ai-crew@bm-plugins
```

บรรทัดที่สองไม่ได้พิมพ์ผิด: `ai-crew` คือชื่อปลั๊กอิน ส่วน `bm-plugins` คือชื่อ **marketplace** ที่ประกาศไว้ในไฟล์ `.claude-plugin/marketplace.json` ซึ่งไม่จำเป็นต้องตรงกับชื่อ repo

### วิธี B — ติดตั้งจากไฟล์ในเครื่อง (ไม่ต้องใช้เน็ต / ใช้เวอร์ชันที่แก้เอง)

1. ดาวน์โหลด zip แล้วแตกไฟล์ไว้ที่ไหนก็ได้ที่ **ไม่ใช่ในโฟลเดอร์โปรเจกต์** เช่น
   `C:\Users\<ชื่อคุณ>\Documents\claude-plugins\bm-plugins`
   ข้างในต้องมีโฟลเดอร์ `.claude-plugin` และโฟลเดอร์ `ai-crew`
2. ใน Terminal พิมพ์ (ชี้ไปที่โฟลเดอร์ที่มี `.claude-plugin` อยู่ข้างใน):

```
claude plugin marketplace add C:\Users\<ชื่อคุณ>\Documents\claude-plugins\bm-plugins
claude plugin install ai-crew@bm-plugins
```

3. **ห้ามลบหรือย้ายโฟลเดอร์นั้น** เพราะเป็นต้นทางที่ Claude Code อ่านตอนอัปเดต

### วิธี C — Claude Cowork

ลากไฟล์ `ai-crew.plugin` เข้าแชท หรือกดปุ่มยอมรับบนการ์ดไฟล์ จบ ไม่ต้องพิมพ์คำสั่ง

### หลังติดตั้งทุกวิธี: ต้องรีสตาร์ท

ปลั๊กอินโหลดตอน Claude Code **เริ่มทำงาน** เท่านั้น session ที่เปิดค้างอยู่จะยังไม่เห็น

- **VS Code:** กด `Ctrl + Shift + P` พิมพ์ `Reload Window` แล้ว Enter จากนั้นในแผง Claude Code กดปุ่ม **+** เปิด session ใหม่
- **CLI:** ปิด `claude` แล้วเปิดใหม่

ตรวจว่าติดตั้งสำเร็จ: พิมพ์ `/crew-setup` ใน **ช่องแชท** ถ้าเห็นมันเริ่มเช็ค Node กับ Codex แสดงว่าใช้ได้แล้ว
ถ้าขึ้น `Unknown command` ให้ลองพิมพ์ชื่อเต็ม `/ai-crew:crew-setup` และดูหัวข้อ [แก้ปัญหา](#8-แก้ปัญหาที่พบบ่อย)

---

## 3. เตรียมเครื่องด้วย /crew-setup

พิมพ์ในช่องแชท **ครั้งเดียวต่อเครื่อง**:

```
/crew-setup
```

มันจะไล่ตรวจให้ 5 ข้อ และหยุดถามเมื่อถึงขั้นที่ต้องให้คุณทำเอง

| ข้อ | มันทำอะไร | คุณต้องทำอะไร |
|---|---|---|
| 1. Node | เช็ค `node -v`, `npm -v` | ถ้าไม่มี ติดตั้ง Node.js LTS แล้วเปิด terminal ใหม่ |
| 2. Config | แสดงว่าตอนนี้ใช้โมเดลและผู้ตรวจอะไรอยู่ | ไม่ต้องทำอะไร |
| 3. ผู้ตรวจ (CLI) | ติดตั้ง `@openai/codex` และ/หรือ `@google/gemini-cli` ให้ | **login เอง** (ดูด้านล่าง) |
| 4. Agents | ลองเรียก agent ของทีมดูว่าโหลดครบ | ถ้า error ว่าไม่พบ agent ให้รีสตาร์ท Claude Code |
| 5. ทดสอบยิงจริง | ส่งคำขอตรวจปลอมไปหาผู้ตรวจ 1 ครั้ง | ดูผลสรุป |

### การ login ผู้ตรวจ (ทำเองใน terminal ครั้งเดียว)

**Codex** — ใช้บัญชี ChatGPT เดียวกับแอป Codex แต่ **แอป Codex กับ Codex CLI คนละตัวกัน** มีแอปแล้วไม่ได้แปลว่ามีคำสั่ง `codex` ใน terminal

```
npm install -g @openai/codex
codex login
```

จะเปิดเบราว์เซอร์ให้เข้าบัญชี ChatGPT เสร็จแล้วกลับมาพิมพ์ในแชทว่า "login แล้ว"

**Gemini** (จะใช้หรือไม่ใช้ก็ได้ เป็นผู้ตรวจสำรอง)

```
npm install -g @google/gemini-cli
gemini
```

รัน `gemini` เปล่าๆ หนึ่งครั้งเพื่อ sign in ด้วยบัญชี Google แล้วกด Ctrl+C ออก

**Antigravity** (`agy` — ถ้าติดตั้ง Antigravity IDE อยู่แล้วจะมี CLI ติดมาด้วย)

```
agy --version
agy
```

รัน `agy` เปล่าๆ หนึ่งครั้งเพื่อ sign in ด้วยบัญชี Google แล้วออก
หมายเหตุทางเทคนิค: สคริปต์จะรัน `agy` โดยปิด stdin เสมอ เพราะ `agy -p` มีบั๊กที่ไม่พ่นผลลัพธ์ออกมาเลยเมื่อถูก redirect stdout ในโหมดที่ stdin ยังรอ TTY อยู่ (issue #76 ของ antigravity-cli)

> **ไม่มี Codex/Gemini ก็ใช้งานได้** ปลั๊กอินจะใช้ agent Opus เป็นผู้ตรวจแทนอัตโนมัติ และจะระบุในรายงานทุกครั้งว่ารอบนั้นใครตรวจและทำไมถึงไม่ได้ใช้ Codex

---

## 4. ตั้งค่าด้วยหน้าเว็บ (แนะนำ)

วิธีที่ง่ายที่สุด ไม่ต้องพิมพ์ JSON เอง ไม่ต้องจำชื่อคีย์ พิมพ์ในช่องแชท **ครั้งเดียวต่อโปรเจกต์**

```
/crew-config ui
```

<p align="center">
  <picture>
    <source media="(prefers-color-scheme: dark)" srcset="docs/config-ui-dark.png">
    <img alt="หน้าตั้งค่า ai-crew" src="docs/config-ui-light.png" width="760">
  </picture>
</p>

เบราว์เซอร์จะเปิดหน้าตั้งค่าให้เอง (ถ้าไม่เปิด ให้ก็อปลิงก์ `http://127.0.0.1:xxxx/?t=...` ที่พิมพ์ออกมาใน terminal ไปเปิดเอง)

> หน้าตั้งค่าจะทำงานอยู่จนกว่าจะกดปุ่ม **ปิดโปรแกรม** ในหน้าเว็บ หรือกด Ctrl+C ใน terminal ระหว่างนั้น Claude Code จะแสดงว่าคำสั่งยังรันอยู่ ถือว่าปกติ

### หน้าตาและสิ่งที่เลือกได้

หน้าเดียว 6 การ์ด เลื่อนลงทีเดียวจบ

**การ์ด 1 — บันทึกไว้ที่ไหน**
เลือกว่าค่านี้ใช้กับโปรเจกต์นี้อย่างเดียว (`<โปรเจกต์>/.crew/config.json`) หรือเป็นค่าเริ่มต้นของทุกโปรเจกต์ในเครื่อง (`~/.claude/ai-crew.json`) ถ้าตั้งทั้งสองที่ ค่าของโปรเจกต์จะชนะ

**การ์ด 2 — โหมด**
กดการ์ด `eco` / `normal` / `strict` แล้วช่องโมเดลในการ์ดที่ 3 **เปลี่ยนตามให้ทันที** จากนั้นจะปรับเองต่อก็ได้ ไม่ถูกล็อก

**การ์ด 3 — โมเดลแต่ละบทบาท**
มี dropdown 3 ช่องต่อบทบาท เรียงจากซ้ายไปขวาเป็นสายสำรอง เช่น `fable → opus → inherit` หมายความว่าลอง Fable ก่อน ถ้าติดลิมิตหรือล่มข้ามไป Opus ถ้ายังไม่ได้ใช้โมเดลของ session ปัจจุบัน ช่องที่ไม่ต้องการเลือก `—` ได้ ครบทั้ง 5 บทบาท: หัวหน้า, เขียนโค้ด, ทดสอบ, หาไฟล์, เอกสาร

**การ์ด 4 — ผู้ตรวจ**
แต่ละแถวมี ติ๊กเลือก · ชื่อ · **ป้ายสถานะสด** · ปุ่มลูกศรขึ้นลงจัดลำดับ · ปุ่มติดตั้ง/Login
ป้ายสถานะมี 4 แบบ

| ป้าย | หมายความว่า | ทำอะไรต่อ |
|---|---|---|
| พร้อมใช้ (เขียว) | ติดตั้งแล้วและ login แล้ว | ไม่ต้องทำอะไร |
| ยังไม่ login (เหลือง) | มี CLI แต่ยังไม่ได้เข้าบัญชี | กดปุ่ม **Login** |
| ติดตั้งแล้ว · สถานะ login ไม่ทราบ (เหลือง) | CLI ตัวนั้นไม่มีคำสั่งเช็คสถานะ ระบบจึงไม่เดาให้ | ถ้าไม่แน่ใจให้กด **Login** สักครั้ง |
| ยังไม่ติดตั้ง (แดง) | ไม่พบคำสั่งในเครื่อง | กดปุ่ม **ติดตั้ง** |

<p align="center">
  <picture>
    <source media="(prefers-color-scheme: dark)" srcset="docs/reviewers-dark.png">
    <img alt="การ์ดผู้ตรวจ พร้อมป้ายสถานะและปุ่ม Login" src="docs/reviewers-light.png" width="760">
  </picture>
</p>

**การ์ด 5 — อื่นๆ**
จำนวนรอบตรวจสูงสุด · ภาษาที่ตอบ (อัตโนมัติ/ไทย/อังกฤษ) · โหมดอัตโนมัติ (ทุกงานที่แก้ไฟล์ / เฉพาะงานที่แตะ 2 ไฟล์ขึ้นไป / ปิด) · นโยบาย git (ไม่ commit เลย / เสนอแล้วถาม / commit เองหลังผ่าน)

**การ์ด 6 — กติกาของโปรเจกต์**
เลือก template ที่จะเขียนลง `.crew/rules.md` โดยระบบ**เดา stack จากไฟล์ในโปรเจกต์ให้ก่อนแล้ว** และเขียนกำกับไว้ว่าอันไหนคือที่ตรวจเจอ ถ้ามีไฟล์ `rules.md` อยู่แล้วจะ**ไม่เขียนทับ** จนกว่าจะติ๊กช่องเขียนทับ (และถึงตอนนั้นก็ยัง backup ให้ก่อน)

### ปุ่ม Login และปุ่มติดตั้ง ทำงานยังไง

กด **Login** → **หน้าต่าง terminal เด้งขึ้นมาจริงๆ** พร้อมรันคำสั่ง login ของเจ้านั้นเอง (`codex login` / `gemini` / `agy`) → CLI ตัวนั้นเปิดหน้าเว็บ login ให้ → BM เข้าบัญชีในหน้าเว็บของ OpenAI หรือ Google → เสร็จแล้วปิดหน้าต่าง → กลับมากดปุ่ม **ตรวจสถานะอีกครั้ง** ในหน้าตั้งค่า ป้ายจะเปลี่ยนเป็นเขียว

กด **ติดตั้ง** → หน้าต่าง terminal เด้งขึ้นมารัน `npm install -g @openai/codex` (หรือของ Gemini) ให้เห็นความคืบหน้า เสร็จแล้วกดตรวจสถานะอีกครั้งเหมือนกัน

> **สำคัญ: หน้านี้ไม่ขอ ไม่เห็น และไม่เก็บรหัสผ่านหรือ token ใดๆ ทั้งสิ้น** มันแค่เรียกคำสั่ง login ของเจ้าของ CLI ขึ้นมาให้ BM ทำเอง ข้อมูลบัญชีอยู่ในมือ CLI ของเจ้านั้นตลอด

### ความปลอดภัยของหน้าตั้งค่า

เนื่องจากเป็น local server ที่สั่งติดตั้งโปรแกรมได้ จึงกันไว้ 4 ชั้น

1. **ผูกกับ 127.0.0.1 เท่านั้น** เครื่องอื่นในวงแลนหรือในเน็ตเข้าไม่ได้
2. **ต้องมี token สุ่มในลิงก์** ถ้าไม่มีจะได้ 403 เว็บอื่นที่เปิดค้างอยู่ในเบราว์เซอร์จึงยิงคำสั่งมาไม่ได้
3. **ปฏิเสธ request ข้าม origin** กัน CSRF
4. **ปิดตัวเองอัตโนมัติ** หลังไม่มีการใช้งาน 30 นาที

### เปิดตรงๆ โดยไม่ผ่านแชทก็ได้

```bash
node "<โฟลเดอร์ปลั๊กอิน>/tools/config-ui.mjs" --project .
node "<โฟลเดอร์ปลั๊กอิน>/tools/config-ui.mjs" --no-open --port 8790
```

`--no-open` คือไม่ต้องเปิดเบราว์เซอร์ให้ (ใช้ลิงก์ที่พิมพ์ออกมาเอง) เหมาะเวลารันผ่าน SSH หรือ WSL

### บันทึกแล้วเกิดอะไรขึ้น

เขียนไฟล์ตามขอบเขตที่เลือก · ถ้ามีไฟล์เดิมจะ backup เป็น `.bak_YYYYMMDD` ก่อนเสมอ (ไม่ทับ backup เดิม) · ถ้าโปรเจกต์มี `.gitignore` และยังไม่มี `.crew/` จะเพิ่มให้ · **มีผลกับ `/crew` ครั้งถัดไปทันที ไม่ต้องรีสตาร์ท Claude Code** เพราะค่าถูกอ่านตอนรัน

---

## 5. ตั้งค่าแบบถามตอบในแชท

ถ้าไม่อยากเปิดเบราว์เซอร์ หรือทำงานผ่าน SSH ใช้แบบถามตอบในแชทได้ ผลลัพธ์เหมือนกันทุกอย่าง

```
/crew-config
```

จะถามทีละข้อแบบกดเลือกได้ ไม่ต้องพิมพ์ JSON เอง

| คำถาม | ตัวเลือก | แนะนำ |
|---|---|---|
| บันทึกที่ไหน | โปรเจกต์นี้ / ทุกโปรเจกต์ | โปรเจกต์นี้ |
| โหมด | ประหยัด / ปกติ / เข้ม | **ปกติ** (ประหยัดไว้ใช้ตอนโควตาใกล้หมด, เข้มไว้ใช้กับงาน auth/เงิน/ย้ายข้อมูล) |
| โมเดลหัวหน้า | Fable→Opus→session / Opus→Sonnet / Sonnet อย่างเดียว | Fable→Opus→session |
| โมเดลลูกน้อง | Sonnet เขียน + Haiku อ่าน / Haiku ทั้งหมด / Opus เขียน | Sonnet เขียน + Haiku อ่าน |
| ผู้ตรวจ (เลือกได้หลายตัว ตามลำดับ) | Codex, Gemini, Antigravity, Opus, Sonnet | Codex → Gemini → Opus |
| จำนวนรอบแก้ | 1 / 3 / 5 / จนกว่าจะผ่าน | 3 |
| โหมดอัตโนมัติ | ทุกงานแก้โค้ด / เฉพาะงานที่แตะ 2 ไฟล์ขึ้นไป / เฉพาะตอนพิมพ์ /crew | ทุกงานแก้โค้ด |
| ภาษาที่ตอบ | อัตโนมัติ / ไทย / อังกฤษ | อัตโนมัติ (ตอบตามภาษาที่คุณพิมพ์) |
| Git | ไม่ commit เลย / เสนอแล้วถาม / commit เองหลังผ่าน | ไม่ commit เลย |
| กติกาโปรเจกต์ | เลือก template ตาม stack | ตามที่มันตรวจเจอ |

คำสั่งเสริม:

- `/crew-config show` — ดูค่าปัจจุบันเฉยๆ ไม่ถามอะไร
- `/crew-config --global` — ตั้งเป็นค่าเริ่มต้นของทุกโปรเจกต์ (เขียนที่ `~/.claude/ai-crew.json`)
- `/crew-config rules` — สร้าง/แก้เฉพาะไฟล์กติกาของโปรเจกต์

### กติกาโปรเจกต์ (.crew/rules.md)

ไฟล์นี้คือสิ่งที่ทำให้ทีม "รู้จักโปรเจกต์ของคุณ" ปลั๊กอินจะเดา stack จากไฟล์ในโปรเจกต์แล้วก็อป template มาให้:

| ตรวจเจอ | template ที่ใช้ | กติกาที่ได้ |
|---|---|---|
| ไฟล์ `.php` / `composer.json` | `php-hostinger` | `php -l` ทุกไฟล์, รักษา CRLF/BOM, ห้ามแตะ `db_config.php`, อัปโหลดเองด้วย SFTP, prepared statement |
| `next.config.*` + โฟลเดอร์ `supabase/` | `nextjs-supabase` | `tsc --noEmit`, RLS ทุกตารางใหม่, migration ไม่แก้ schema สด, ห้ามแตะ `.env` |
| `package.json` / `pyproject.toml` | `node-python-generic` | รันเทสของโปรเจกต์, ห้ามเพิ่ม dependency เอง |
| อย่างอื่น | `generic` | ให้คุณเติมคำสั่งตรวจ syntax และวิธี deploy เอง |

เปิดไฟล์ `.crew/rules.md` แล้วแก้ได้ตลอด มีช่อง `<...>` ให้เติมไม่กี่จุด ทีมจะอ่านไฟล์นี้ทุกครั้งและส่งท่อนกติกาให้ลูกน้องทุกตัว

---

## 6. เริ่มใช้งานจริง

### แบบอัตโนมัติ (ไม่ต้องพิมพ์คำสั่ง)

เปิดโหมดอัตโนมัติไว้แล้ว แค่บอกงานตามปกติ:

```
เพิ่มปุ่มรีโหลดในหน้ารายการงานซ่อม
```

ถ้าเป็นงานที่ต้องแก้ไฟล์ ทีมจะรับเอง และขึ้นบรรทัดเดียวว่า "ทีมรับงานแล้ว"
ถ้าเป็นคำถาม เช่น "หน้านี้ดึงข้อมูลจาก API ไหน" จะตอบตรงๆ ไม่เรียกทีม

อยากข้ามทีมเป็นครั้งๆ ให้ใส่คำว่า **"ไม่ต้องใช้ทีม"** ต่อท้าย

### แบบสั่งเอง

```
/crew แก้บั๊กหน้าเช็คประกันที่แสดงวันหมดอายุผิด
```

ใส่ flag ต่อท้ายได้: `--eco` (ประหยัด) `--strict` (เข้ม) `--rounds 5` `--lang en` `--no-auto`

### ทำงานค้างต่อ

ถ้าโมเดลติดลิมิตกลางทาง หรือปิดไปแล้วกลับมาใหม่:

```
/model            (เลือกโมเดลอื่น)
ทำต่อ              (หรือพิมพ์ /crew-resume)
```

ทีมจะอ่าน `.crew/state.md` แล้วทำต่อจากจุดเดิม ไม่ทำงานที่เสร็จแล้วซ้ำ

### สิ่งที่จะได้ตอนจบ

รายงานหน้าเดียว บอกว่า: แต่ละโมเดลทำอะไร (คนละไม่เกิน 3 บรรทัด) · ผู้ตรวจคนไหนตรวจและเจออะไร · **รายชื่อไฟล์ที่เปลี่ยนพร้อม path เต็มและไฟล์ backup** · **อะไรทดสอบแล้วจริงพร้อมผลรัน กับอะไรที่คุณต้องไปกดดูเอง** · เรื่องที่ต้องให้คุณตัดสินใจ

---

## 7. ไฟล์ที่ปลั๊กอินสร้างในโปรเจกต์

ทั้งหมดอยู่ในโฟลเดอร์ `.crew/` และจะถูกเพิ่มใน `.gitignore` ให้อัตโนมัติ **ไม่ต้องอัปโหลดขึ้น server**

| ไฟล์ | คืออะไร |
|---|---|
| `.crew/state.md` | ความจำของทีม — โจทย์ แผน ไฟล์ที่แก้ ผลตรวจแต่ละรอบ หลักฐานที่รันจริง เปิดอ่านได้ตลอด |
| `.crew/config.json` | ค่าตั้งของโปรเจกต์นี้ |
| `.crew/rules.md` | กติกาของโปรเจกต์ |
| `.crew/review-request.md` | คำขอตรวจที่ส่งให้ผู้ตรวจรอบล่าสุด |
| `.crew/review-N-codex.md` | ผลตรวจรอบที่ N |

ส่วนไฟล์โค้ดของคุณที่ถูกแก้ จะมี backup คู่กันเสมอ ชื่อ `<ชื่อไฟล์>.bak_YYYYMMDD` ถ้าอยากย้อนกลับก็ก็อปทับได้เลย

---

## 8. ตั้งค่าละเอียด (config)

ปกติใช้ `/crew-config` ก็พอ แต่ถ้าอยากแก้มือ ไฟล์อยู่ที่ `.crew/config.json`

```json
{
  "language": "th",
  "mode": "normal",
  "models": {
    "lead":   ["fable", "opus", "inherit"],
    "coder":  ["sonnet", "haiku"],
    "tester": ["sonnet", "haiku"],
    "scout":  ["haiku", "sonnet"],
    "writer": ["haiku", "sonnet"]
  },
  "reviewers": ["codex", "gemini", "opus"],
  "max_rounds": 3,
  "double_review": false,
  "auto_mode": true,
  "auto_mode_min_files": 1,
  "rules_file": ".crew/rules.md",
  "git": { "commit": "never" },
  "deploy": { "manual": true, "list_changed_files": true }
}
```

**ค่าโมเดลเป็นรายการสำรอง** เช่น `["fable","opus","inherit"]` แปลว่าลอง Fable ก่อน ถ้าติดลิมิต/ล่ม ข้ามไป Opus ถ้ายังไม่ได้ใช้โมเดลของ session ปัจจุบัน — ทุกครั้งที่สลับจะจดไว้ในรายงาน `inherit` = โมเดลที่คุณเลือกไว้ใน session

**ลำดับความสำคัญของค่าตั้ง** (ล่างทับบน): ค่าเริ่มต้นของปลั๊กอิน → โหมด → `~/.claude/ai-crew.json` → `.crew/config.json` → flag ที่พิมพ์ตอนสั่ง

คำอธิบายทุก key อยู่ใน `ai-crew/skills/crew/references/config-schema.md`

---

## 9. แก้ปัญหาที่พบบ่อย

| อาการ | สาเหตุ | วิธีแก้ |
|---|---|---|
| พิมพ์ `/plugin marketplace add ...` ในแชทแล้วขึ้น *"/plugin isn't available in this environment"* | แผงแชทของ extension ไม่รองรับคำสั่ง `/plugin` | ใช้ terminal แทน: `claude plugin marketplace add ...` |
| พิมพ์ `/crew-setup` ใน terminal แล้วขึ้น *"is not recognized as the name of a cmdlet"* | `/crew-*` เป็นคำสั่งของแชท ไม่ใช่ของ PowerShell | พิมพ์ในช่องแชทของ Claude Code |
| `claude` ไม่ใช่คำสั่งที่รู้จัก | มีแต่ extension ยังไม่มี CLI | `npm install -g @anthropic-ai/claude-code` แล้วเปิด terminal ใหม่ |
| อัปเดตแล้วแต่ยังใช้ของเก่า / `Unknown command: /crew-setup` | Claude Code โหลดปลั๊กอินตอนเริ่มเท่านั้น | Reload Window แล้วเปิด session ใหม่ (ปุ่ม +) |
| ยังไม่รู้จัก `/crew` แต่รู้จัก `/ai-crew:crew` | ชื่อสั้นชนกับอย่างอื่น | ใช้ชื่อเต็ม `/ai-crew:crew` ได้เลย |
| ทีมไม่ทำงานอัตโนมัติ ต้องพิมพ์ `/crew` ตลอด | hook ยังไม่ทำงาน (session เก่า) หรือ `auto_mode: false` | Reload + session ใหม่ · ตรวจด้วย `/crew-config show` · ลองถาม CC ว่าเห็นบรรทัด `[ai-crew auto]` ในข้อความล่าสุดไหม |
| ผู้ตรวจขึ้น `REVIEWER_NOT_FOUND` | ยังไม่ติดตั้ง CLI นั้น | `npm install -g @openai/codex` หรือ `@google/gemini-cli` (agy มากับ Antigravity IDE) |
| ผู้ตรวจ `agy` ขึ้น `REVIEWER_ERROR ... produced no output` | บั๊กของ `agy -p` ตอนถูก redirect (issue #76) หรือเวอร์ชันเก่า | ทีมจะข้ามไปผู้ตรวจตัวถัดไปเองอัตโนมัติ · อัปเดต Antigravity CLI ให้ใหม่สุด · หรือเอา `antigravity` ออกจากรายการผู้ตรวจด้วย `/crew-config` |
| ขึ้น `REVIEWER_NOT_LOGGED_IN` | ติดตั้งแล้วแต่ยังไม่ได้ login | `codex login` หรือรัน `gemini` หนึ่งครั้ง |
| ขึ้น `REVIEWER_RATE_LIMITED` | โควตาของบัญชีนั้นหมดชั่วคราว | ไม่ต้องทำอะไร ทีมจะใช้ผู้ตรวจตัวถัดไป (Gemini แล้ว Opus) และระบุในรายงาน |
| งานค้างกลางทางเพราะโมเดลติดลิมิต | โควตา Claude หมด | `/model` เลือกโมเดลอื่น แล้วพิมพ์ "ทำต่อ" — ทีมอ่าน `.crew/state.md` ทำต่อได้ทันที |
| ทีมจุกจิกเกินไปกับงานเล็ก | โหมดอัตโนมัติรับทุกงาน | `/crew-config` เลือก "เฉพาะงานที่แตะ 2 ไฟล์ขึ้นไป" หรือพิมพ์ "ไม่ต้องใช้ทีม" เป็นครั้งๆ |
| อยากย้อนไฟล์ที่ทีมแก้ | มี backup อยู่แล้ว | ก็อป `<ไฟล์>.bak_YYYYMMDD` ทับไฟล์เดิม |
| `/crew-config ui` แล้วเบราว์เซอร์ไม่เปิด | เปิดเบราว์เซอร์อัตโนมัติไม่ได้ | ก็อปลิงก์ `http://127.0.0.1:xxxx/?t=...` ที่พิมพ์ใน terminal ไปเปิดเอง |
| เปิดหน้าตั้งค่าแล้วขึ้น 403 / bad token | ลิงก์ไม่มี token หรือใช้ลิงก์เก่าหลังรีสตาร์ท | ใช้ลิงก์ล่าสุดที่พิมพ์ออกมา token เปลี่ยนทุกครั้งที่เปิดใหม่ |
| กดปุ่ม Login แล้วหน้าต่างไม่เด้ง | เปิด terminal อัตโนมัติไม่ได้บนเครื่องนั้น | หน้าเว็บจะแสดงคำสั่งให้ ก็อปไปรันใน terminal เองแล้วกดตรวจสถานะอีกครั้ง |
| ป้ายยังเหลืองทั้งที่ login แล้ว | CLI นั้นไม่มีคำสั่งเช็คสถานะ ระบบจึงไม่เดา | ใช้งานได้ตามปกติ ถ้าตรวจจริงแล้วติดจะขึ้น `REVIEWER_NOT_LOGGED_IN` ในรายงาน |
| หน้าตั้งค่าหายไปเอง | ปิดอัตโนมัติหลังไม่ใช้งาน 30 นาที | พิมพ์ `/crew-config ui` ใหม่ |

---

## 10. อัปเดตและถอนการติดตั้ง

**อัปเดต** (เมื่อมีเวอร์ชันใหม่บน GitHub หรือแก้ไฟล์ต้นฉบับในเครื่องเอง):

```
claude plugin update ai-crew@bm-plugins
```

แล้ว **Reload Window + เปิด session ใหม่** เสมอ (ข้อความจะบอกว่า *"Restart to apply changes"*)

> ถ้าแก้ไฟล์ต้นฉบับเองต้อง **เพิ่มเลข version** ใน `ai-crew/.claude-plugin/plugin.json` และ `.claude-plugin/marketplace.json` ก่อน ไม่งั้นคำสั่ง update จะคิดว่าไม่มีอะไรเปลี่ยน
> เหตุผล: Claude Code ไม่ได้รันจากโฟลเดอร์ต้นฉบับ แต่ก็อปไปไว้ที่ `~/.claude/plugins/cache/bm-plugins/ai-crew/<version>/`

**ถอนการติดตั้ง:**

```
claude plugin uninstall ai-crew@bm-plugins
claude plugin marketplace remove bm-plugins
```

โฟลเดอร์ `.crew/` ในโปรเจกต์ยังอยู่ ลบเองได้ถ้าไม่ต้องการแล้ว

---

## 10.5 ใช้กับ Antigravity IDE ได้ไหม

Antigravity ของ Google เป็น IDE ที่ fork มาจาก VS Code และรองรับ extension ของ VS Code ดังนั้น **ใช้ได้ 2 ทาง**:

1. **ผ่าน terminal ใน Antigravity** — วิธีที่ชัวร์ที่สุด เพราะ `claude` เป็น CLI ธรรมดา เปิด terminal ใน Antigravity แล้วพิมพ์ `claude` ได้เลย ปลั๊กอินทำงานครบทุกอย่างเหมือนใน VS Code
2. **ติดตั้ง extension Claude Code ใน Antigravity** — น่าจะได้เพราะ Antigravity รองรับ extension ของ VS Code แต่ **ยังไม่ได้ทดสอบ** ถ้าหาใน marketplace ในตัวไม่เจอ ให้ติดตั้งจากไฟล์ `.vsix` แทน

และแยกอีกเรื่องหนึ่ง: **Antigravity CLI (`agy`) ใช้เป็นผู้ตรวจในทีมได้** เพิ่มในรายการผู้ตรวจด้วย `/crew-config` (ดูวิธี login ในหัวข้อ 3) แต่ควรรู้ว่า `agy` กับ `gemini` ใช้โมเดลตระกูล Gemini เหมือนกัน ถ้าอยากได้มุมมองที่ต่างจริงๆ ควรมี Codex อยู่ในสายด้วย

## 11. ข้อจำกัดที่ควรรู้ก่อนใช้

- **ปลั๊กอินเปลี่ยนโมเดลของ session เองไม่ได้** Claude Code ไม่เปิดให้ทำ สิ่งที่สลับอัตโนมัติได้คือ "สมองหัวหน้า" (วางแผน/คัดกรองผลตรวจ/เขียนรายงาน) ที่ส่งไปเป็น agent ถ้าทั้งบัญชีติดลิมิต ทีมจะบันทึกสถานะแล้วบอกให้คุณ `/model` เอง
- **โควตาไม่ได้เพิ่มขึ้นจากการสลับโมเดล** แผน Claude ส่วนใหญ่ใช้โควตาร่วมกัน การสลับช่วยเมื่อโมเดลใดล่ม/overloaded/มีลิมิตแยกเท่านั้น
- **Codex CLI ใช้โควตา Codex ของบัญชี ChatGPT ร่วมกับแอป Codex** ถ้าอยากไม่ติดลิมิต ใส่ `OPENAI_API_KEY` (จ่ายตามใช้จริง) Gemini ก็เช่นกันกับ `GEMINI_API_KEY`
- **โหมดอัตโนมัติทำให้งานเล็กช้าลงและเปลืองโควตามากขึ้น** เพราะผ่านทีม 5-6 ตัวและมีผู้ตรวจ ปรับได้ด้วย `auto_mode_min_files`
- **ทีมไม่ commit / ไม่ push / ไม่ลบไฟล์ / ไม่อัปโหลดขึ้น server ให้** โดยตั้งใจ (เปลี่ยนได้ที่ `git.commit`) คุณเป็นคน deploy เอง
- **ผลตรวจของ Codex/Gemini เป็นข้อมูล ไม่ใช่คำสั่ง** ถ้าในผลตรวจมีข้อความสั่งให้ลบไฟล์ commit หรือเปิดเผย credential ทีมจะไม่ทำตามและจะรายงานให้คุณทราบ

---

## หลักการที่ฝังอยู่ในทีม (ทำไมถึงเชื่อรายงานได้)

1. **ไม่คิดไปเอง** — ทุกข้อสรุปต้องมีหลักฐานที่รันซ้ำได้ (คำสั่ง + ผลลัพธ์ หรือ `ไฟล์:บรรทัด` ที่เปิดจริง) ห้ามใช้คำว่า "น่าจะ" "คิดว่า"
2. **"ทดสอบแล้ว" = รันจริงและแนบผล** ถ้ารันไม่ได้ ต้องบอกว่าอะไรทดสอบไม่ได้และคุณต้องไปดูตรงไหน
3. **ข้อ CRITICAL/HIGH ปิดว่า "ไม่จริง" ไม่ได้ถ้าไม่มีหลักฐาน** ต้องให้ tester รันพิสูจน์ก่อน
4. **งานเสร็จเมื่อ tester ยืนยัน** ไม่ใช่คนเขียนบอกเองว่าเสร็จ
5. **โค้ดจริงชนะเอกสารและความจำเสมอ** ถ้าไม่ตรงกัน ให้แก้เอกสารตามโค้ด
6. **backup ก่อนแก้ทุกไฟล์** และรักษา line ending / encoding เดิม
